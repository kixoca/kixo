- unless professional.nil?
  .professional.loop.minimal
    .container
      .row
        .three.columns
          = link_to professional_path(professional) do
            .headshot-2.thumb{:style => "background-image: url(#{professional.headshot.url(:thumb)});"}
            =# image_tag professional.headshot.url(:thumb), :class => "headshot"
        .nine.columns
          = link_to professional_path(professional), :class => "name" do
            = professional.name
          .whois
            %span.professions
              = professional.professions_list