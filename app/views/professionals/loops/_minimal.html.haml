- unless professional.nil?
  .professional.loop.minimal
    .container
      .row
        .four.columns
          %a.headshot{:href => professional_path(professional)}
            = image_tag professional.headshot.url(:small)
        .eight.columns
          %a.name{:href => professional_path(professional)}
            = professional.name