- if controller_name != 'sessions'
  .panel.callout.green
    %h5 Already have an account?
    %p No need to create a new account! Sign in to your existing one.
    %a.button{:href => new_user_session_path} Sign in

- if devise_mapping.registerable? && controller_name != 'registrations'
  .panel.callout.green
    %h5 Don't have an account?
    %p You can join for free today and benefit from all the features.
    %p
      %a.button{:href => new_registration_path(resource_name)} Sign up

.panel
  %h5 Not a professional?
  %p You might want to go over the regular user section.
  %ul.button-group
    %li
      %a.button{:href => new_user_session_path} Sign in
    %li
      %a.button{:href => new_user_registration_path} Sign up